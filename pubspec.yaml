name: testapp
description: A sample web application
dependencies:
  browser: any
  angular:
    git:
      url: git://github.com/chirayuk/angular.dart
      ref: master
  http_server: ">=0.9.2 < 0.10.0"

transformers:
- angular
